name: CI/CD
on:
  push:
    branches: [ main ]

jobs:
  continuous-integration:
    runs-on: self-hosted
    steps:
      # Step 1
      - uses: actions/checkout@v2

  continuous-deployment:
    runs-on: self-hosted
    needs: [continuous-integration]
    if: github.ref == 'refs/heads/main'  
    steps:
     # Step 1
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }} AKIASTIR46DL2K75POMB 
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }} 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
          aws-region: ${{ secrets.AWS_REGION }} 
     # Step 2
      - name: Create CodeDeploy Deployment
      #  id: deploy
        run: |
            
            cd ~/edulink-backend/
            # if [[ -d "~/edulink-backend" ]]
            #   then
            #     sudo rm -rf ~/edulink-backend
            # fi
               
            #  git clone https://github.com/Basimohd/edulink-backend.git
            #  cd edulink-backend
            git reset --hard
            git pull origin main
              # docker container stop $(docker container ls -aq)
              # docker rm -f $(docker ps -aq) 
            docker rmi -f heet
            docker-compose down
            docker build -t heet .
            docker-compose up -d